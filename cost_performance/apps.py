from django.apps import AppConfig


class CostPerformanceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "cost_performance"
    verbose_name = "Project Cost Performance (EVM)"
