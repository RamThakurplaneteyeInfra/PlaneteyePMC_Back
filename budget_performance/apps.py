from django.apps import AppConfig


class BudgetPerformanceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "budget_performance"
    verbose_name = "Budget vs Cost Performance (EVM)"
